# 营销工具 · 产品知识库 — 详细设计

本文档说明「产品知识库」的信息架构、界面区块与参考图映射，便于与 `assets/knowledge1.jpg`～`knowledge6.jpg` 像素级对照验收。

## 1. 参考图与页面对照（一比一验收）

请将设计稿置于：`assets/knowledge1.jpg` … `knowledge6.jpg`。

| 参考图（均在 `assets/`） | 对应实现文件 | 说明 |
|---------------------------|--------------|------|
| `knowledge1.jpg` | `marketing-knowledge.html` · Tab **首页** · 子 Tab **全局搜索** | **知识专区**顶栏、`总行规章制度系列` 下拉、「知」+ **知识库**、**全局搜索 \| AI智搜 \| 知识导航**、本模式下搜索框+按钮、**热门知识**+换一批、排行 1～10（前三色标 +「热」）、斜纹水印氛围 |
| `knowledge2.jpg` | 同上 · Tab **专家问答** | **专家问答**顶栏、关键字搜索、全部问答/我的提问、排序、空状态「暂无回复」、右下角 **提问** FAB（无三子 Tab） |
| `knowledge3.jpg` | `marketing-knowledge-ask.html` | 提问表单页（问题名称/分类/说明、附图「+」、提交） |
| `knowledge4.jpg` | `marketing-knowledge.html` · Tab **我的** | 顶栏「专家问答」、渐变弧形顶区、头像 **73**、**李佳琪** / **307773**、我的收藏 / 我的提问 / 反馈建议 |
| `knowledge5.jpg` | 同上 · Tab **首页** · 子 Tab **AI智搜** | 下拉切换为 **`企业电子渠道系列`**；大卡：气泡「Hi，我是AI助理小海～」、海豚区、两行引导文案；蓝色描边输入框、相册 + 灰色发送键；底部内测免责 |
| `knowledge6.jpg` | 同上 · Tab **首页** · 子 Tab **知识导航** | 下拉 **`企业电子渠道系列`**；左侧约 **30%** 类目；右侧 **投产公告 / 业务通知 / 常见问题** + 圆角标签 **指导文档、e 盾** 等 |

深链（首页子模式）：`marketing-knowledge.html?homeMode=ai`、`…?homeMode=nav`。专家问答列表：`?tab=qa`。

验收方式：打开 `marketing-knowledge.html`，对照对应 Tab，逐项比对间距、字号层级、分割线、图标类型；若与设计稿不一致，优先以 JPG 为准调整 Tailwind 类名或局部 CSS。

## 2. 全局导航与路由

- **入口**：`marketing-center.html` → 「产品知识库」→ `marketing-knowledge.html`
- **壳体**：与其它营销中心页一致 — `styles.css` 中 `.phone-shell`，底部主导航仍为工作台 / 客户中心 / **营销中心**（激活）/ 流程中心 / 管理中心
- **知识库内导航**：顶部固定 **三段 Tab**：`首页` | `专家问答` | `我的`（仅切换内容区，不改变底部五大 Tab）

## 3. Tab：首页（对齐 knowledge1）

### 3.1 区域拆分（自上而下）

1. **页面标题栏**：左侧标题「知识库」或「产品知识库」（与营销中心命名一致），右侧可选「搜索」图标（若稿中为图标入口则保留）
2. **搜索**：占位文案「搜索知识标题、关键字」；后续对接全文检索接口
3. **Banner**：轮播指示点；支持配置营销专题、活动图（原型为占位渐变图）
4. **知识分类**：2×2 或 1×4 宫格 — 图标 + 文案（示例：信贷产品、普惠金融、结算与现金管理、政策法规）
5. **热门推荐**：横向滚动卡片（标题 + 摘要一行 + 阅读量/时间）
6. **最新知识**：纵向列表 — 左侧小封面或色块占位，右侧标题、两行摘要、底部标签（分类）与时间
7. **列表项交互**：点击进入 **知识详情**（同页抽屉/全屏层或独立 `marketing-knowledge-detail.html`）；原型在 `marketing-knowledge.html` 内用浮层演示

### 3.2 首页数据（原型假数据）

- 分类：`id`、名称、图标类名（Font Awesome）、跳转 `categoryId`
- 推荐/列表：`id`、标题、摘要、标签、发布时间、阅读量

## 4. Tab：专家问答（对齐 knowledge3）

### 4.1 区块说明

1. **筛选条**：全部 / 待解答 / 已采纳（或稿中同款 chip），单选切换列表
2. **列表项**：主标题（问题）、1～2 行摘要、右侧或下方展示专家名/「官方」标记、回答数、发布时间
3. **主按钮**：底部固定「我要提问」（或稿中悬浮按钮位置），点击弹出占位或跳转提问页（原型 `alert`/表单浮层）

### 4.2 交互

- 点击某条进入 **问答详情**（问题全文 + 回答列表）；原型可用同页第二层内容区
- 「我要提问」：提交后落入「我的 → 我的提问」列表（示意）

## 5. Tab：我的（对齐 knowledge4）

### 5.1 区块说明

1. **用户信息区**：头像、姓名、机构/条线（占位）
2. **数据条**（若稿中有）：收藏数、浏览数、提问数等
3. **功能列表**（标准行式）  
   - 我的收藏  
   - 浏览历史  
   - 我的提问  
   - 下载/离线（若有）  
   - 意见反馈（可选）
4. 每行右侧箭头；点击 toast 或占位详情（原型）

## 6. 知识详情（承接首页列表）

- **字段**：标题、发布时间、来源部门、正文（富文本占位）、附件列表（文件名 + 下载）
- **操作**：收藏、分享（占位）

## 7. 权限与非功能（预留）

- 营销条线全员可读；敏感类目可按角色过滤列表（接口字段 `visibleRoles`）
- 问答提交需登录身份（走统一 SSO）；附件下载审计日志（后端）

## 8. 接口占位（REST）

| 能力 | 方法 | 路径示例 |
|------|------|----------|
| 首页聚合 | GET | `/api/marketing/knowledge/home` |
| 分类下列表 | GET | `/api/marketing/knowledge/articles?categoryId=` |
| 文章详情 | GET | `/api/marketing/knowledge/articles/{id}` |
| 问答列表 | GET | `/api/marketing/knowledge/qa?filter=` |
| 提交提问 | POST | `/api/marketing/knowledge/qa` |
| 我的收藏 | GET | `/api/marketing/knowledge/me/favorites` |

---

实现文件：**`marketing-knowledge.html`**（三 Tab + 详情浮层）。对照 **`assets/knowledge1.jpg`～`knowledge4.jpg`** 做最终像素微调。
